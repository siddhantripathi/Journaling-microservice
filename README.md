Auto runs on chrome. We can change that in run.py

To execute:

1. cd backend

2. pip install -r requirements.txt

3. cd ..

4. cd frontend

5. flutter pub get

6. cd ..

7. python run.py

You will need to add .env file to backend directory
